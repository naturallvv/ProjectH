# ── 서버 ──
APP_ENV=development
CORS_ORIGINS=http://localhost:5173

# ── RAG 연동 (별도 팀원 서버) ──
# 비워 두면 백엔드가 mock 설명(fallback)을 반환한다.
RAG_SERVER_URL=
RAG_TIMEOUT_SECONDS=5

# ── 공공데이터 API 키 (Step 14 실연동용, 지금은 미사용) ──
KMA_API_KEY=
JDC_API_KEY=
DATA_GO_KR_API_KEY=
